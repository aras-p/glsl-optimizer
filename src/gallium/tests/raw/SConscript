Import('*')

if 'graw-xlib' not in env['winsys']:
    Return()

env = env.Clone()

env.Prepend(LIBPATH = [graw.dir])
env.Prepend(LIBS = [graw.name])

progs = [
    'clear'
]

for prog in progs:
    env.Program(
        target = prog,
        source = prog + '.c',
    )

