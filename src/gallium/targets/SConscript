Import('*')

#if env['dri']:
#	SConscript([
#		'drm/SConscript',
#	])
	
if 'xlib' in env['targets']:
	SConscript([
		'libgl-xlib/SConscript',
	])

#if 'gdi' in env['winsys']:
#	SConscript([
#		'gdi/SConscript',
#	])
