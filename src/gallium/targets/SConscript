Import('*')

#if env['dri']:
#	SConscript([
#		'drm/SConscript',
#	])
	
if 'xlib' in env['winsys']:
	SConscript([
		'libgl-xlib/SConscript',
	])

#if 'gdi' in env['winsys']:
#	SConscript([
#		'gdi/SConscript',
#	])
