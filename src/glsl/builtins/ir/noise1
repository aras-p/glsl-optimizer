((function noise1
   (signature float
     (parameters
       (declare (in) float x))
     ((return (constant float (0)))))
   (signature float
     (parameters
       (declare (in) vec2 x))
     ((return (constant float (0)))))
   (signature float
     (parameters
       (declare (in) vec3 x))
     ((return (constant float (0)))))
   (signature float
     (parameters
       (declare (in) vec4 x))
     ((return (constant float (0)))))
))
