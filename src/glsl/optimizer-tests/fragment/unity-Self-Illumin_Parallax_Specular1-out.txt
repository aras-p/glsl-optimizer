struct SurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  float Specular;
  float Gloss;
  float Alpha;
};
struct Input {
  vec2 uv_MainTex;
  vec2 uv_BumpMap;
  vec2 uv_Illum;
  vec3 viewDir;
};
varying vec4 xlv_FOG;
uniform vec4 _SpecColor;
uniform float _Shininess;
uniform sampler2D _ParallaxMap;
uniform float _Parallax;
uniform sampler2D _MainTex;
uniform sampler2D _LightTextureB0;
uniform samplerCube _LightTexture0;
uniform vec4 _LightColor0;
uniform sampler2D _Illum;
uniform vec4 _Color;
uniform sampler2D _BumpMap;
void main ()
{
  vec4 tmpvar_132;
  vec3 tmpvar_133;
  vec3 tmpvar_135;
  tmpvar_132 = gl_TexCoord[0].xyzw;
  tmpvar_133 = gl_TexCoord[1].xyz;
  tmpvar_135 = gl_TexCoord[3].xyz;
  vec4 c;
  SurfaceOutput o;
  Input surfIN;
  surfIN.uv_MainTex = tmpvar_132.xy;
  surfIN.uv_BumpMap = tmpvar_132.zw;
  surfIN.viewDir = tmpvar_133;
  o.Albedo = vec3(0.000000, 0.000000, 0.000000);
  o.Emission = vec3(0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  o.Gloss = 0.000000;
  Input IN_i0;
  IN_i0 = surfIN;
  SurfaceOutput o_i0;
  o_i0 = o;
  vec2 _ret_val_i0;
  vec3 v;
  vec3 tmpvar_80;
  tmpvar_80 = normalize (surfIN.viewDir);
  v = tmpvar_80;
  v.z = float(vec3((tmpvar_80.z + 0.420000)));
  _ret_val_i0 = (((texture2D (_ParallaxMap, surfIN.uv_BumpMap).w * _Parallax) - (_Parallax / 2.000000)) * (v.xy / v.z));
  IN_i0.uv_MainTex = (surfIN.uv_MainTex + _ret_val_i0);
  IN_i0.uv_BumpMap = (IN_i0.uv_BumpMap + _ret_val_i0);
  IN_i0.uv_Illum = (IN_i0.uv_Illum + _ret_val_i0);
  vec4 tmpvar_87;
  tmpvar_87 = texture2D (_MainTex, IN_i0.uv_MainTex);
  vec4 tmpvar_89;
  tmpvar_89 = (tmpvar_87 * _Color);
  o_i0.Albedo = tmpvar_89.xyz;
  o_i0.Gloss = tmpvar_87.w;
  o_i0.Emission = (tmpvar_89.xyz * texture2D (_Illum, IN_i0.uv_Illum).w);
  o_i0.Specular = _Shininess;
  o_i0.Alpha = tmpvar_89.w;
  vec4 normal;
  normal.xy = ((texture2D (_BumpMap, IN_i0.uv_BumpMap).wy * 2.000000) - 1.000000).xy;
  normal.z = float(vec3(sqrt (((1.000000 - (normal.x * normal.x)) - (normal.y * normal.y)))));
  o_i0.Normal = normal.xyz;
  o = o_i0;
  vec3 tmpvar_105;
  tmpvar_105 = normalize (gl_TexCoord[2].xyz);
  float atten;
  atten = (texture2D (_LightTextureB0, vec2(dot (tmpvar_135, tmpvar_135))).w * textureCube (_LightTexture0, tmpvar_135).w);
  vec4 c_i0_i1;
  float tmpvar_125;
  tmpvar_125 = (pow (max (0.000000, dot (o_i0.Normal, normalize ((tmpvar_105 + normalize (tmpvar_133.xyz))))), (o_i0.Specular * 128.000000)) * o_i0.Gloss);
  c_i0_i1.xyz = ((((o_i0.Albedo * _LightColor0.xyz) * max (0.000000, dot (o_i0.Normal, tmpvar_105))) + ((_LightColor0.xyz * _SpecColor.xyz) * tmpvar_125)) * (atten * 2.000000)).xyz;
  c_i0_i1.w = float(vec4((o_i0.Alpha + (((_LightColor0.w * _SpecColor.w) * tmpvar_125) * atten))));
  c = c_i0_i1;
  c.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  gl_FragData[0] = c.xyzw;
}

