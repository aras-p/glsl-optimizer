uniform sampler2D _MainTex;
uniform sampler2D _LightMap;
uniform vec4 _Color;
uniform sampler2D _BumpMap;
void main ()
{
  vec4 res;
  vec3 viewN;
  vec4 normal;
  normal.xy = ((texture2D (_BumpMap, gl_TexCoord[0].xy).wy * 2.000000) - 1.000000).xy;
  normal.z = float(vec3(sqrt (((1.000000 - (normal.x * normal.x)) - (normal.y * normal.y)))));
  vec3 tmpvar_53;
  tmpvar_53 = normal.xyz;
  viewN.x = dot (gl_TexCoord[1].xyz, tmpvar_53);
  viewN.y = float(vec2(dot (gl_TexCoord[2].xyz, tmpvar_53)));
  viewN.z = float(vec3(dot (gl_TexCoord[3].xyz, tmpvar_53)));
  res.xyz = ((viewN * vec3(0.500000, 0.500000, -0.500000)) + 0.500000).xyz;
  res.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  gl_FragData[0] = res.xyzw;
}

