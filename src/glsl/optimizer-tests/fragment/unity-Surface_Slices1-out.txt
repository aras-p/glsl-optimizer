varying vec4 xlv_FOG;
uniform vec4 unity_Ambient;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
void main ()
{
  vec3 tmpvar_5;
  tmpvar_5 = gl_TexCoord[1].xyz;
  vec4 tmpvar_7;
  tmpvar_7 = gl_TexCoord[2].xyzw;
  vec4 light;
  vec2 tmpvar_20;
  tmpvar_20 = gl_TexCoord[0].xy;
  float x;
  x = (fract (((tmpvar_5.y + (tmpvar_5.z * 0.100000)) * 5.000000)) - 0.500000);
  if ((x < 0.000000)) {
    discard;
  };
  vec4 tmpvar_33;
  tmpvar_33 = -(log2 (texture2DProj (_LightBuffer, tmpvar_7)));
  light = tmpvar_33;
  light.xyz = (tmpvar_33.xyz + unity_Ambient.xyz).xyz;
  vec4 c;
  c.xyz = (texture2D (_MainTex, tmpvar_20).xyz * light.xyz).xyz;
  c.w = (vec4(0.000000, 0.000000, 0.000000, 0.000000)).w;
  gl_FragData[0] = c.xyzw;
}

