varying vec4 xlv_FOG;
uniform sampler2D unity_LightmapInd;
uniform sampler2D unity_Lightmap;
uniform vec4 _SpecColor;
uniform float _Shininess;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
uniform vec4 _Color;
uniform sampler2D _BumpMap;
void main ()
{
  vec3 tmpvar_7;
  tmpvar_7 = gl_TexCoord[2].xyz;
  vec4 light;
  vec2 tmpvar_102;
  vec4 tmpvar_46;
  tmpvar_46 = texture2D (_MainTex, gl_TexCoord[0].xy);
  vec4 normal;
  normal.xy = ((texture2D (_BumpMap, tmpvar_102).wy * 2.000000) - 1.000000);
  normal.z = (vec3(sqrt (((1.000000 - (normal.x * normal.x)) - (normal.y * normal.y))))).z;
  vec4 tmpvar_63;
  tmpvar_63 = -(log2 (texture2DProj (_LightBuffer, gl_TexCoord[1].xyzw)));
  light = tmpvar_63;
  light.xyz = (tmpvar_63.xyz + mix ((2.000000 * texture2D (unity_LightmapInd, tmpvar_7.xy).xyz), (2.000000 * texture2D (unity_Lightmap, tmpvar_7.xy).xyz), vec3(clamp (tmpvar_7.z, 0.000000, 1.000000)))).xyz;
  vec4 c;
  float tmpvar_81;
  tmpvar_81 = (light.w * tmpvar_46.w);
  c.xyz = (((tmpvar_46.xyz * _Color.xyz) * light.xyz) + ((light.xyz * _SpecColor.xyz) * tmpvar_81)).xyz;
  c.w = (vec4(((tmpvar_46.w * _Color.w) + (tmpvar_81 * _SpecColor.w)))).w;
  gl_FragData[0] = c.xyzw;
}

