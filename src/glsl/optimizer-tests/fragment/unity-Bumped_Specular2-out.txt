struct SurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  float Specular;
  float Gloss;
  float Alpha;
};
struct Input {
  vec2 uv_MainTex;
  vec2 uv_BumpMap;
};
struct v2f_surf {
  vec4 pos;
  float fog;
  vec2 hip_pack0;
  vec4 hip_screen;
  vec3 hip_lmapFade;
};
varying vec4 xlv_FOG;
uniform sampler2D unity_LightmapInd;
uniform sampler2D unity_Lightmap;
uniform vec4 _SpecColor;
uniform float _Shininess;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
uniform vec4 _Color;
uniform sampler2D _BumpMap;
void main ()
{
  v2f_surf xlt_IN;
  xlt_IN.pos = vec4(0.000000, 0.000000, 0.000000, 0.000000);
  xlt_IN.fog = xlv_FOG.x;
  xlt_IN.hip_pack0 = gl_TexCoord[0].xy;
  xlt_IN.hip_screen = gl_TexCoord[1].xyzw;
  xlt_IN.hip_lmapFade = gl_TexCoord[2].xyz;
  v2f_surf IN;
  IN = xlt_IN;
  vec3 lmIndirect;
  vec3 lmFull;
  vec4 light;
  SurfaceOutput o;
  Input surfIN;
  surfIN.uv_MainTex = xlt_IN.hip_pack0.xy;
  o.Albedo = vec3(0.000000, 0.000000, 0.000000);
  o.Emission = vec3(0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  o.Gloss = 0.000000;
  Input IN_i0;
  IN_i0 = surfIN;
  SurfaceOutput o_i0;
  o_i0 = o;
  vec4 tmpvar_46;
  tmpvar_46 = texture2D (_MainTex, surfIN.uv_MainTex);
  o_i0.Albedo = (tmpvar_46.xyz * _Color.xyz);
  o_i0.Gloss = tmpvar_46.w;
  o_i0.Alpha = (tmpvar_46.w * _Color.w);
  o_i0.Specular = _Shininess;
  vec4 tmpvar_52;
  tmpvar_52 = texture2D (_BumpMap, IN_i0.uv_BumpMap);
  vec4 normal;
  normal.xy = ((tmpvar_52.wy * 2.000000) - 1.000000).xy;
  float tmpvar_56;
  tmpvar_56 = sqrt (((1.000000 - (normal.x * normal.x)) - (normal.y * normal.y)));
  normal.z = float(vec3(tmpvar_56));
  o_i0.Normal = normal.xyz;
  o = o_i0;
  vec4 tmpvar_60;
  tmpvar_60 = texture2DProj (_LightBuffer, IN.hip_screen);
  light = tmpvar_60;
  vec4 tmpvar_62;
  tmpvar_62 = log2 (tmpvar_60);
  light = -(tmpvar_62);
  vec4 tmpvar_64;
  tmpvar_64 = texture2D (unity_Lightmap, IN.hip_lmapFade.xy);
  lmFull = (2.000000 * tmpvar_64.xyz);
  vec4 tmpvar_68;
  tmpvar_68 = texture2D (unity_LightmapInd, IN.hip_lmapFade.xy);
  lmIndirect = (2.000000 * tmpvar_68.xyz);
  float tmpvar_74;
  tmpvar_74 = clamp (IN.hip_lmapFade.z, 0.000000, 1.000000);
  vec3 tmpvar_76;
  tmpvar_76 = mix (lmIndirect, lmFull, vec3(tmpvar_74));
  light.xyz = (light.xyz + tmpvar_76).xyz;
  vec4 c;
  float tmpvar_81;
  tmpvar_81 = (light.w * o.Gloss);
  c.xyz = ((o.Albedo * light.xyz) + ((light.xyz * _SpecColor.xyz) * tmpvar_81)).xyz;
  c.w = float(vec4((o.Alpha + (tmpvar_81 * _SpecColor.w))));
  gl_FragData[0] = c.xyzw;
}

