struct v2f {
  vec4 pos;
  float fog;
  vec4 uv;
  vec4 color;
};
uniform float _HalfOverCutoff;
uniform sampler2D _MainTex;
varying vec4 xlv_FOG;
void main ()
{
  v2f xlt_xlat_var_input;
  xlt_xlat_var_input.pos = vec4 (0.000000, 0.000000, 0.000000, 0.000000);
  xlt_xlat_var_input.fog = xlv_FOG.x;
  vec4 tmpvar_3;
  tmpvar_3.xyzw = gl_TexCoord[0];
  xlt_xlat_var_input.uv = tmpvar_3;
  vec4 tmpvar_5;
  tmpvar_5.xyzw = gl_Color;
  xlt_xlat_var_input.color = tmpvar_5;
  v2f xlat_var_input;
  xlat_var_input = xlt_xlat_var_input;
  vec4 col;
  vec4 tmpvar_12;
  tmpvar_12 = texture2D (_MainTex, xlat_var_input.uv.xy);
  col = tmpvar_12;
  col.xyz = (tmpvar_12.xyz * (2.000000 * xlat_var_input.color.xyz));
  col.w = (col.w * (2.000000 * _HalfOverCutoff));
  float x;
  x = (col.w - 1.000000);
  if ((x < 0.000000)) {
    discard;
  };
  vec4 tmpvar_9;
  tmpvar_9.xyzw = col;
  gl_FragData[0] = tmpvar_9;
}

