varying vec4 xlv_FOG;
uniform sampler2D _MainTex;
uniform float _HalfOverCutoff;
void main ()
{
  vec4 col;
  vec4 tmpvar_12;
  tmpvar_12 = texture2D (_MainTex, gl_TexCoord[0].xy);
  col = tmpvar_12;
  col.xyz = (tmpvar_12.xyz * (2.000000 * gl_Color.xyz)).xyz;
  col.w = (vec4((col.w * (2.000000 * _HalfOverCutoff)))).w;
  float x;
  x = (col.w - 1.000000);
  if ((x < 0.000000)) {
    discard;
  };
  gl_FragData[0] = col.xyzw;
}

