struct LeafSurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  vec3 Translucency;
  float Specular;
  float Gloss;
  float Alpha;
};
struct v2f_surf {
  vec4 pos;
  float fog;
  vec2 hip_pack0;
  vec4 lop_color;
  vec3 lightDir;
  vec3 viewDir;
  vec3 _LightCoord;
};
varying vec4 xlv_FOG;
uniform sampler2D _TranslucencyMap;
uniform sampler2D _MainTex;
uniform sampler2D _LightTextureB0;
uniform samplerCube _LightTexture0;
uniform vec4 _LightColor0;
uniform float _Cutoff;
uniform sampler2D _BumpSpecMap;
void main ()
{
  v2f_surf xlt_IN;
  xlt_IN.pos = vec4(0.000000, 0.000000, 0.000000, 0.000000);
  xlt_IN.fog = xlv_FOG.x;
  xlt_IN.hip_pack0 = gl_TexCoord[0].xy;
  xlt_IN.lop_color = gl_Color.xyzw;
  xlt_IN.lightDir = gl_TexCoord[1].xyz;
  xlt_IN.viewDir = gl_TexCoord[2].xyz;
  xlt_IN._LightCoord = gl_TexCoord[3].xyz;
  v2f_surf IN;
  IN = xlt_IN;
  vec4 c;
  LeafSurfaceOutput o;
  vec2 tmpvar_125;
  vec4 tmpvar_126;
  tmpvar_125 = xlt_IN.hip_pack0.xy;
  tmpvar_126 = xlt_IN.lop_color;
  o.Albedo = vec3(0.000000, 0.000000, 0.000000);
  o.Emission = vec3(0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  LeafSurfaceOutput o_i0;
  o_i0 = o;
  vec4 tmpvar_71;
  tmpvar_71 = texture2D (_MainTex, tmpvar_125);
  o_i0.Albedo = (tmpvar_71.xyz * tmpvar_126.xyz);
  vec4 tmpvar_74;
  tmpvar_74 = texture2D (_TranslucencyMap, tmpvar_125);
  o_i0.Translucency = tmpvar_74.xyz;
  o_i0.Gloss = tmpvar_74.w;
  o_i0.Alpha = (tmpvar_71.w * tmpvar_126.w);
  vec4 tmpvar_79;
  tmpvar_79 = texture2D (_BumpSpecMap, tmpvar_125);
  o_i0.Specular = tmpvar_79.x;
  vec4 normal;
  normal.xy = ((tmpvar_79.wy * 2.000000) - 1.000000).xy;
  normal.z = float(vec3(sqrt (((1.000000 - (normal.x * normal.x)) - (normal.y * normal.y)))));
  o_i0.Normal = normal.xyz;
  o = o_i0;
  float x;
  x = (o_i0.Alpha - _Cutoff);
  if ((x < 0.000000)) {
    discard;
  };
  vec3 tmpvar_90;
  tmpvar_90 = normalize (IN.lightDir);
  vec4 c_i0_i1;
  float tmpvar_103;
  tmpvar_103 = dot (o.Normal, tmpvar_90);
  float tmpvar_110;
  tmpvar_110 = max (0.000000, -(tmpvar_103));
  c_i0_i1.xyz = ((((o.Albedo * (max (0.000000, ((tmpvar_103 * 0.500000) + 0.500000)) + ((o.Translucency * tmpvar_110) * 2.000000))) + ((pow (max (0.000000, dot (o.Normal, normalize ((tmpvar_90 + normalize (IN.viewDir.xyz))))), (o.Specular * 128.000000)) * o.Gloss) * (1.000000 - clamp (ceil (tmpvar_110), 0.000000, 1.000000)))) * _LightColor0.xyz) * ((texture2D (_LightTextureB0, vec2(dot (IN._LightCoord, IN._LightCoord))).w * textureCube (_LightTexture0, IN._LightCoord).w) * 2.000000)).xyz;
  c = c_i0_i1;
  c.w = float(vec4(o.Alpha));
  gl_FragData[0] = c.xyzw;
}

