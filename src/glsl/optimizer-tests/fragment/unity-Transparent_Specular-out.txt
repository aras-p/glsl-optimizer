struct SurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  float Specular;
  float Gloss;
  float Alpha;
};
struct v2f_surf {
  vec4 pos;
  float fog;
  vec2 hip_pack0;
  vec3 normal;
  vec3 lightDir;
  vec3 viewDir;
  vec2 _LightCoord;
};
varying vec4 xlv_FOG;
uniform vec4 _SpecColor;
uniform float _Shininess;
uniform sampler2D _MainTex;
uniform sampler2D _LightTexture0;
uniform vec4 _LightColor0;
uniform vec4 _Color;
void main ()
{
  v2f_surf xlt_IN;
  xlt_IN.pos = vec4(0.000000, 0.000000, 0.000000, 0.000000);
  xlt_IN.fog = xlv_FOG.x;
  xlt_IN.hip_pack0 = gl_TexCoord[0].xy;
  xlt_IN.normal = gl_TexCoord[1].xyz;
  xlt_IN.lightDir = gl_TexCoord[2].xyz;
  xlt_IN.viewDir = gl_TexCoord[3].xyz;
  xlt_IN._LightCoord = gl_TexCoord[4].xy;
  v2f_surf IN;
  IN = xlt_IN;
  vec4 c;
  vec3 lightDir;
  SurfaceOutput o;
  o.Albedo = vec3(0.000000, 0.000000, 0.000000);
  o.Emission = vec3(0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  o.Gloss = 0.000000;
  o.Normal = xlt_IN.normal;
  SurfaceOutput o_i0;
  o_i0 = o;
  vec4 tmpvar_44;
  tmpvar_44 = texture2D (_MainTex, xlt_IN.hip_pack0.xy);
  o_i0.Albedo = (tmpvar_44.xyz * _Color.xyz);
  o_i0.Gloss = tmpvar_44.w;
  o_i0.Alpha = (tmpvar_44.w * _Color.w);
  o_i0.Specular = _Shininess;
  o = o_i0;
  lightDir = IN.lightDir;
  vec3 tmpvar_52;
  tmpvar_52 = normalize (IN.viewDir.xyz);
  vec4 tmpvar_53;
  tmpvar_53 = texture2D (_LightTexture0, IN._LightCoord);
  SurfaceOutput s;
  s = o;
  vec3 lightDir_i0;
  lightDir_i0 = lightDir;
  float atten;
  atten = tmpvar_53.w;
  vec4 c_i0;
  float diff;
  vec3 h;
  vec3 tmpvar_56;
  tmpvar_56 = normalize ((lightDir + tmpvar_52));
  h = tmpvar_56;
  float tmpvar_58;
  tmpvar_58 = dot (s.Normal, lightDir_i0);
  float tmpvar_59;
  tmpvar_59 = max (0.000000, tmpvar_58);
  diff = tmpvar_59;
  float tmpvar_61;
  tmpvar_61 = dot (s.Normal, h);
  float tmpvar_62;
  tmpvar_62 = max (0.000000, tmpvar_61);
  float tmpvar_64;
  tmpvar_64 = pow (tmpvar_62, (s.Specular * 128.000000));
  float tmpvar_65;
  tmpvar_65 = (tmpvar_64 * s.Gloss);
  c_i0.xyz = ((((s.Albedo * _LightColor0.xyz) * diff) + ((_LightColor0.xyz * _SpecColor.xyz) * tmpvar_65)) * (atten * 2.000000)).xyz;
  c_i0.w = float(vec4((s.Alpha + (((_LightColor0.w * _SpecColor.w) * tmpvar_65) * atten))));
  c = c_i0;
  c.w = float(vec4(o.Alpha));
  gl_FragData[0] = c.xyzw;
}

