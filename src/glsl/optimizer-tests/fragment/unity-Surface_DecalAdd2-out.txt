struct SurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  float Specular;
  float Gloss;
  float Alpha;
};
varying vec4 xlv_FOG;
uniform sampler2D unity_LightmapInd;
uniform sampler2D unity_Lightmap;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
void main ()
{
  vec3 tmpvar_59;
  tmpvar_59 = gl_TexCoord[2].xyz;
  vec4 light;
  SurfaceOutput o;
  o.Albedo = vec3(0.000000, 0.000000, 0.000000);
  o.Emission = vec3(0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  o.Gloss = 0.000000;
  SurfaceOutput o_i0;
  o_i0 = o;
  o_i0.Albedo = (texture2D (_MainTex, gl_TexCoord[0].xy).xyz * 0.500000);
  o = o_i0;
  vec4 tmpvar_34;
  tmpvar_34 = -(log2 (texture2DProj (_LightBuffer, gl_TexCoord[1].xyzw)));
  light = tmpvar_34;
  light.xyz = (tmpvar_34.xyz + mix ((2.000000 * texture2D (unity_LightmapInd, tmpvar_59.xy).xyz), (2.000000 * texture2D (unity_Lightmap, tmpvar_59.xy).xyz), vec3(clamp (tmpvar_59.z, 0.000000, 1.000000)))).xyz;
  vec4 c;
  c.xyz = (o_i0.Albedo * light.xyz).xyz;
  c.w = float(vec4(o_i0.Alpha));
  gl_FragData[0] = c.xyzw;
}

