varying vec4 xlv_FOG;
uniform sampler2D unity_LightmapInd;
uniform sampler2D unity_Lightmap;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
void main ()
{
  vec3 tmpvar_7;
  tmpvar_7 = gl_TexCoord[2].xyz;
  vec4 light;
  vec4 tmpvar_34;
  tmpvar_34 = -(log2 (texture2DProj (_LightBuffer, gl_TexCoord[1].xyzw)));
  light = tmpvar_34;
  light.xyz = (tmpvar_34.xyz + mix ((2.0 * texture2D (unity_LightmapInd, tmpvar_7.xy).xyz), (2.0 * texture2D (unity_Lightmap, tmpvar_7.xy).xyz), vec3(clamp (tmpvar_7.z, 0.0, 1.0)))).xyz;
  vec4 c;
  c.xyz = ((texture2D (_MainTex, gl_TexCoord[0].xy).xyz * 0.5) * light.xyz).xyz;
  c.w = 0.0;
  gl_FragData[0] = c.xyzw;
}

