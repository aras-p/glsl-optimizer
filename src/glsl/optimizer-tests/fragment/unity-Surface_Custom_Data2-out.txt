varying vec4 xlv_FOG;
uniform sampler2D unity_LightmapInd;
uniform sampler2D unity_Lightmap;
uniform sampler2D _LightBuffer;
void main ()
{
  vec3 tmpvar_7;
  tmpvar_7 = gl_TexCoord[2].xyz;
  vec4 light;
  vec4 tmpvar_32;
  tmpvar_32 = -(log2 (texture2DProj (_LightBuffer, gl_TexCoord[1].xyzw)));
  light = tmpvar_32;
  light.xyz = (tmpvar_32.xyz + mix ((2.000000 * texture2D (unity_LightmapInd, tmpvar_7.xy).xyz), (2.000000 * texture2D (unity_Lightmap, tmpvar_7.xy).xyz), vec3(clamp (tmpvar_7.z, 0.000000, 1.000000)))).xyz;
  vec4 c;
  c.xyz = (gl_TexCoord[0].xyz * light.xyz).xyz;
  c.w = 0.000000;
  gl_FragData[0] = c.xyzw;
}

