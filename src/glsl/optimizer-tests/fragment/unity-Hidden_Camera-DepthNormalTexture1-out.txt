uniform sampler2D _MainTex;
uniform float _Cutoff;
void main ()
{
  vec4 tmpvar_4;
  tmpvar_4 = gl_TexCoord[1].xyzw;
  float x;
  x = (texture2D (_MainTex, gl_TexCoord[0].xy).w - _Cutoff);
  if ((x < 0.0)) {
    discard;
  };
  vec3 normal;
  normal = tmpvar_4.xyz;
  vec4 enc;
  enc.xy = ((((normal.xy / (normal.z + 1.0)) / 1.777700) * 0.500000) + 0.500000);
  vec2 enc_i0_i1;
  vec2 tmpvar_56;
  tmpvar_56 = fract ((vec2(1.0, 255.0) * tmpvar_4.w));
  enc_i0_i1 = tmpvar_56;
  enc_i0_i1.x = (tmpvar_56.x - (tmpvar_56.y * 0.003922));
  enc.zw = enc_i0_i1.xy;
  gl_FragData[0] = enc.xyzw;
}

