struct v2f {
  vec4 vertex;
  vec4 color;
  vec2 texcoord;
};
uniform sampler2D _MainTex;
uniform vec4 _TintColor;
void main ()
{
  v2f xlt_i;
  xlt_i.vertex = vec4 (0.000000, 0.000000, 0.000000, 0.000000);
  vec4 tmpvar_2;
  tmpvar_2.xyzw = gl_Color;
  xlt_i.color = tmpvar_2;
  vec2 tmpvar_4;
  tmpvar_4.xy = vec2(gl_TexCoord[0]);
  xlt_i.texcoord = tmpvar_4;
  v2f i;
  i = xlt_i;
  vec4 col;
  vec4 tmpvar_11;
  tmpvar_11 = texture2D (_MainTex, i.texcoord);
  col.xyz = (((_TintColor.xyz * tmpvar_11.xyz) * i.color.xyz) * 2.000000);
  col.w = ((1.000000 - tmpvar_11.w) * ((_TintColor.w * i.color.w) * 2.000000));
  vec4 tmpvar_8;
  tmpvar_8.xyzw = col;
  gl_FragData[0] = tmpvar_8;
}

