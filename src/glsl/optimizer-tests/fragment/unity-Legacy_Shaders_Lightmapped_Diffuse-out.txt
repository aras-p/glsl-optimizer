struct SurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  float Specular;
  float Gloss;
  float Alpha;
};
struct Input {
  vec2 uv_MainTex;
  vec2 uv2_LightMap;
};
struct v2f_surf {
  vec4 pos;
  float fog;
  vec2 hip_pack0;
  vec3 normal;
  vec3 lightDir;
  vec4 _LightCoord;
};
uniform vec4 _Color;
uniform vec4 _LightColor0;
uniform sampler2D _LightMap;
uniform sampler2D _LightTexture0;
uniform sampler2D _LightTextureB0;
uniform sampler2D _MainTex;
varying vec4 xlv_FOG;
void main ()
{
  v2f_surf xlt_IN;
  xlt_IN.pos = vec4 (0.000000, 0.000000, 0.000000, 0.000000);
  xlt_IN.fog = xlv_FOG.x;
  vec2 tmpvar_3;
  tmpvar_3.xy = vec2(gl_TexCoord[0]);
  xlt_IN.hip_pack0 = tmpvar_3;
  vec3 tmpvar_5;
  tmpvar_5.xyz = vec3(gl_TexCoord[1]);
  xlt_IN.normal = tmpvar_5;
  vec3 tmpvar_7;
  tmpvar_7.xyz = vec3(gl_TexCoord[2]);
  xlt_IN.lightDir = tmpvar_7;
  vec4 tmpvar_9;
  tmpvar_9.xyzw = gl_TexCoord[3];
  xlt_IN._LightCoord = tmpvar_9;
  v2f_surf IN;
  IN = xlt_IN;
  Input surfIN;
  SurfaceOutput o;
  vec3 lightDir;
  vec4 c;
  surfIN.uv_MainTex = xlt_IN.hip_pack0.xy;
  o.Albedo = vec3 (0.000000, 0.000000, 0.000000);
  o.Emission = vec3 (0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  o.Gloss = 0.000000;
  o.Normal = xlt_IN.normal;
  Input IN_i0;
  IN_i0 = surfIN;
  SurfaceOutput o_i0;
  o_i0 = o;
  vec4 tmpvar_43;
  tmpvar_43 = texture2D (_MainTex, IN_i0.uv_MainTex);
  vec3 tmpvar_44;
  tmpvar_44.xyz = vec3(_Color);
  o_i0.Albedo = (tmpvar_43.xyz * tmpvar_44);
  vec4 tmpvar_46;
  tmpvar_46 = texture2D (_LightMap, IN_i0.uv2_LightMap);
  o_i0.Emission = (tmpvar_46.xyz * o_i0.Albedo.xyz);
  o_i0.Alpha = (tmpvar_46.w * _Color.w);
  o = o_i0;
  lightDir = IN.lightDir;
  vec3 tmpvar_51;
  tmpvar_51 = normalize (lightDir);
  lightDir = tmpvar_51;
  float tmpvar_53;
  vec4 LightCoord;
  LightCoord = IN._LightCoord;
  vec4 tmpvar_55;
  tmpvar_55 = texture2D (_LightTexture0, ((LightCoord.xy / LightCoord.w) + 0.500000));
  tmpvar_53 = tmpvar_55.w;
  vec3 LightCoord_i0;
  LightCoord_i0 = IN._LightCoord.xyz;
  float tmpvar_58;
  tmpvar_58 = dot (LightCoord_i0, LightCoord_i0);
  vec2 tmpvar_60;
  tmpvar_60.xy = vec2(tmpvar_58);
  vec4 tmpvar_61;
  tmpvar_61 = texture2D (_LightTextureB0, tmpvar_60);
  SurfaceOutput s;
  s = o;
  float atten;
  atten = ((float((IN._LightCoord.z > 0.000000)) * tmpvar_53) * tmpvar_61.w);
  vec4 c_i0;
  float tmpvar_64;
  tmpvar_64 = dot (s.Normal, lightDir);
  float tmpvar_65;
  tmpvar_65 = max (0.000000, tmpvar_64);
  c_i0.xyz = ((s.Albedo * _LightColor0.xyz) * ((tmpvar_65 * atten) * 2.000000));
  c_i0.w = s.Alpha;
  c = c_i0;
  c.w = 0.000000;
  vec4 tmpvar_13;
  tmpvar_13.xyzw = c;
  gl_FragData[0] = tmpvar_13;
}

