uniform float _Shininess;
uniform sampler2D _MainTex;
uniform float _Cutoff;
uniform vec4 _Color;
uniform sampler2D _BumpMap;
void main ()
{
  vec4 tmpvar_2;
  tmpvar_2 = gl_TexCoord[0].xyzw;
  vec3 tmpvar_70;
  vec3 tmpvar_71;
  vec3 tmpvar_72;
  tmpvar_70 = gl_TexCoord[1].xyz;
  tmpvar_71 = gl_TexCoord[2].xyz;
  tmpvar_72 = gl_TexCoord[3].xyz;
  vec4 res;
  vec3 viewN;
  vec3 tmpvar_74;
  float tmpvar_76;
  vec4 normal;
  normal.xy = ((texture2D (_BumpMap, tmpvar_2.zw).wy * 2.000000) - 1.000000).xy;
  normal.z = float(vec3(sqrt (((1.000000 - (normal.x * normal.x)) - (normal.y * normal.y)))));
  tmpvar_74 = normal.xyz;
  tmpvar_76 = _Shininess;
  float x;
  x = ((texture2D (_MainTex, tmpvar_2.xy).w * _Color.w) - _Cutoff);
  if ((x < 0.000000)) {
    discard;
  };
  viewN.x = dot (tmpvar_70, tmpvar_74);
  viewN.y = float(vec2(dot (tmpvar_71, tmpvar_74)));
  viewN.z = float(vec3(dot (tmpvar_72, tmpvar_74)));
  tmpvar_74 = viewN;
  res.xyz = ((viewN * vec3(0.500000, 0.500000, -0.500000)) + 0.500000).xyz;
  res.w = float(vec4(tmpvar_76));
  gl_FragData[0] = res.xyzw;
}

