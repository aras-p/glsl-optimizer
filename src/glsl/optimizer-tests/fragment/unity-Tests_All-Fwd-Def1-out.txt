struct SurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  float Specular;
  float Gloss;
  float Alpha;
};
struct v2f_surf {
  vec4 pos;
  float fog;
  vec4 hip_screen;
  vec3 hip_lmapFade;
};
uniform sampler2D _LightBuffer;
uniform sampler2D unity_Lightmap;
uniform sampler2D unity_LightmapInd;
varying vec4 xlv_FOG;
void main ()
{
  v2f_surf xlt_IN;
  xlt_IN.pos = vec4 (0.000000, 0.000000, 0.000000, 0.000000);
  xlt_IN.fog = xlv_FOG.x;
  vec4 tmpvar_3;
  tmpvar_3.xyzw = gl_TexCoord[0];
  xlt_IN.hip_screen = tmpvar_3;
  vec3 tmpvar_5;
  tmpvar_5.xyz = vec3(gl_TexCoord[1]);
  xlt_IN.hip_lmapFade = tmpvar_5;
  v2f_surf IN;
  IN = xlt_IN;
  SurfaceOutput o;
  vec4 light;
  vec3 lmFull;
  vec3 lmIndirect;
  vec4 col;
  o.Albedo = vec3 (0.000000, 0.000000, 0.000000);
  o.Emission = vec3 (0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  o.Gloss = 0.000000;
  SurfaceOutput o_i0;
  o_i0 = o;
  o_i0.Emission = vec3 (1.000000, 0.000000, 0.000000);
  o = o_i0;
  vec4 tmpvar_26;
  tmpvar_26 = texture2DProj (_LightBuffer, IN.hip_screen);
  light = tmpvar_26;
  vec4 tmpvar_28;
  tmpvar_28 = log2 (light);
  light = -(tmpvar_28);
  vec4 tmpvar_30;
  tmpvar_30 = texture2D (unity_Lightmap, IN.hip_lmapFade.xy);
  lmFull = (2.000000 * tmpvar_30.xyz);
  vec4 tmpvar_34;
  tmpvar_34 = texture2D (unity_LightmapInd, IN.hip_lmapFade.xy);
  lmIndirect = (2.000000 * tmpvar_34.xyz);
  float tmpvar_40;
  tmpvar_40 = clamp (IN.hip_lmapFade.z, 0.000000, 1.000000);
  vec3 tmpvar_42;
  tmpvar_42 = mix (lmIndirect, lmFull, vec3(tmpvar_40));
  light.xyz = (light.xyz + tmpvar_42);
  vec4 c;
  c.xyz = (o.Albedo * light.xyz);
  c.w = o.Alpha;
  col = c;
  col.xyz = (c.xyz + o.Emission);
  vec4 tmpvar_9;
  tmpvar_9.xyzw = col;
  gl_FragData[0] = tmpvar_9;
}

