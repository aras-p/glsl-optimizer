struct SurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  float Specular;
  float Gloss;
  float Alpha;
};
struct Input {
  vec2 uv_MainTex;
};
struct v2f_surf {
  vec4 pos;
  float fog;
  vec2 hip_pack0;
  vec3 normal;
  vec3 lightDir;
  vec4 _LightCoord;
};
uniform vec4 _Color;
uniform float _Cutoff;
uniform vec4 _LightColor0;
uniform sampler2D _LightTexture0;
uniform sampler2D _LightTextureB0;
uniform sampler2D _MainTex;
varying vec4 xlv_FOG;
void main ()
{
  v2f_surf xlt_IN;
  xlt_IN.pos = vec4 (0.000000, 0.000000, 0.000000, 0.000000);
  xlt_IN.fog = xlv_FOG.x;
  xlt_IN.hip_pack0 = gl_TexCoord[0].xy;
  xlt_IN.normal = gl_TexCoord[1].xyz;
  xlt_IN.lightDir = gl_TexCoord[2].xyz;
  xlt_IN._LightCoord = gl_TexCoord[3].xyzw;
  v2f_surf IN;
  IN = xlt_IN;
  Input surfIN;
  SurfaceOutput o;
  vec3 lightDir;
  vec4 c;
  surfIN.uv_MainTex = xlt_IN.hip_pack0.xy;
  o.Albedo = vec3 (0.000000, 0.000000, 0.000000);
  o.Emission = vec3 (0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  o.Gloss = 0.000000;
  o.Normal = xlt_IN.normal;
  Input IN_i0;
  IN_i0 = surfIN;
  SurfaceOutput o_i0;
  o_i0 = o;
  vec4 tmpvar_40;
  tmpvar_40 = texture2D (_MainTex, IN_i0.uv_MainTex);
  vec4 tmpvar_41;
  tmpvar_41 = (tmpvar_40 * _Color);
  o_i0.Albedo = tmpvar_41.xyz;
  o_i0.Alpha = tmpvar_41.w;
  o = o_i0;
  float x;
  x = (o_i0.Alpha - _Cutoff);
  if ((x < 0.000000)) {
    discard;
  };
  lightDir = IN.lightDir;
  vec3 tmpvar_45;
  tmpvar_45 = normalize (lightDir);
  lightDir = tmpvar_45;
  float tmpvar_47;
  vec4 LightCoord;
  LightCoord = IN._LightCoord;
  vec4 tmpvar_49;
  tmpvar_49 = texture2D (_LightTexture0, ((LightCoord.xy / LightCoord.w) + 0.500000));
  tmpvar_47 = tmpvar_49.w;
  vec3 LightCoord_i0;
  LightCoord_i0 = IN._LightCoord.xyz;
  float tmpvar_52;
  tmpvar_52 = dot (LightCoord_i0, LightCoord_i0);
  vec4 tmpvar_55;
  tmpvar_55 = texture2D (_LightTextureB0, vec2(tmpvar_52));
  SurfaceOutput s;
  s = o;
  float atten;
  atten = ((float((IN._LightCoord.z > 0.000000)) * tmpvar_47) * tmpvar_55.w);
  vec4 c_i0_i1;
  float tmpvar_58;
  tmpvar_58 = dot (s.Normal, lightDir);
  float tmpvar_59;
  tmpvar_59 = max (0.000000, tmpvar_58);
  c_i0_i1.xyz = ((s.Albedo * _LightColor0.xyz) * ((tmpvar_59 * atten) * 2.000000)).xyz;
  c_i0_i1.w = float(vec4(s.Alpha));
  c = c_i0_i1;
  c.w = float(vec4(o.Alpha));
  gl_FragData[0] = c.xyzw;
}

