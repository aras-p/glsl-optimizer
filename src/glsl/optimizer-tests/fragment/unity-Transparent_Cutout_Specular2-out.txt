varying vec4 xlv_FOG;
uniform sampler2D unity_LightmapInd;
uniform sampler2D unity_Lightmap;
uniform vec4 _SpecColor;
uniform float _Shininess;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
uniform float _Cutoff;
uniform vec4 _Color;
void main ()
{
  vec4 tmpvar_5;
  tmpvar_5 = gl_TexCoord[1].xyzw;
  vec3 tmpvar_7;
  tmpvar_7 = gl_TexCoord[2].xyz;
  vec4 light;
  vec4 tmpvar_34;
  tmpvar_34 = texture2D (_MainTex, gl_TexCoord[0].xy);
  vec3 tmpvar_36;
  tmpvar_36 = (tmpvar_34.xyz * _Color.xyz);
  float tmpvar_37;
  tmpvar_37 = tmpvar_34.w;
  float tmpvar_38;
  tmpvar_38 = (tmpvar_34.w * _Color.w);
  float x;
  x = (tmpvar_38 - _Cutoff);
  if ((x < 0.0)) {
    discard;
  };
  vec4 tmpvar_43;
  tmpvar_43 = -(log2 (texture2DProj (_LightBuffer, tmpvar_5)));
  light = tmpvar_43;
  light.xyz = (tmpvar_43.xyz + mix ((2.0 * texture2D (unity_LightmapInd, tmpvar_7.xy).xyz), (2.0 * texture2D (unity_Lightmap, tmpvar_7.xy).xyz), vec3(clamp (tmpvar_7.z, 0.0, 1.0)))).xyz;
  vec4 c;
  float tmpvar_61;
  tmpvar_61 = (light.w * tmpvar_37);
  c.xyz = ((tmpvar_36 * light.xyz) + ((light.xyz * _SpecColor.xyz) * tmpvar_61)).xyz;
  c.w = (vec4((tmpvar_38 + (tmpvar_61 * _SpecColor.w)))).w;
  gl_FragData[0] = c.xyzw;
}

