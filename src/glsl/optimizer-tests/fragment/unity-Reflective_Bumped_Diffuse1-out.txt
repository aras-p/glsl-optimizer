struct SurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  float Specular;
  float Gloss;
  float Alpha;
};
varying vec4 xlv_FOG;
uniform sampler2D unity_LightmapInd;
uniform sampler2D unity_Lightmap;
uniform vec4 _ReflectColor;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
uniform samplerCube _Cube;
uniform vec4 _Color;
uniform sampler2D _BumpMap;
void main ()
{
  vec4 tmpvar_116;
  vec4 tmpvar_118;
  vec4 tmpvar_119;
  vec4 tmpvar_120;
  vec3 tmpvar_121;
  tmpvar_116 = gl_TexCoord[0].xyzw;
  tmpvar_118 = gl_TexCoord[2].xyzw;
  tmpvar_119 = gl_TexCoord[3].xyzw;
  tmpvar_120 = gl_TexCoord[4].xyzw;
  tmpvar_121 = gl_TexCoord[5].xyz;
  vec4 col;
  vec4 light;
  SurfaceOutput o;
  vec3 tmpvar_56;
  tmpvar_56.x = float(tmpvar_118.w);
  tmpvar_56.y = float(tmpvar_119.w);
  tmpvar_56.z = float(tmpvar_120.w);
  o.Albedo = vec3(0.000000, 0.000000, 0.000000);
  o.Emission = vec3(0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  o.Gloss = 0.000000;
  SurfaceOutput o_i0;
  o_i0 = o;
  vec4 tmpvar_66;
  tmpvar_66 = texture2D (_MainTex, tmpvar_116.xy);
  o_i0.Albedo = (tmpvar_66 * _Color).xyz;
  vec4 normal;
  normal.xy = ((texture2D (_BumpMap, tmpvar_116.zw).wy * 2.000000) - 1.000000).xy;
  normal.z = float(vec3(sqrt (((1.000000 - (normal.x * normal.x)) - (normal.y * normal.y)))));
  o_i0.Normal = normal.xyz;
  vec3 tmpvar_81;
  tmpvar_81.x = float(vec3(dot (tmpvar_118.xyz, o_i0.Normal)));
  tmpvar_81.y = float(vec3(dot (tmpvar_119.xyz, o_i0.Normal)));
  tmpvar_81.z = float(vec3(dot (tmpvar_120.xyz, o_i0.Normal)));
  vec4 tmpvar_86;
  tmpvar_86 = (textureCube (_Cube, reflect (tmpvar_56, tmpvar_81)) * tmpvar_66.w);
  o_i0.Emission = (tmpvar_86.xyz * _ReflectColor.xyz);
  o_i0.Alpha = (tmpvar_86.w * _ReflectColor.w);
  o = o_i0;
  vec4 tmpvar_92;
  tmpvar_92 = -(log2 (texture2DProj (_LightBuffer, gl_TexCoord[1].xyzw)));
  light = tmpvar_92;
  light.xyz = (tmpvar_92.xyz + mix ((2.000000 * texture2D (unity_LightmapInd, tmpvar_121.xy).xyz), (2.000000 * texture2D (unity_Lightmap, tmpvar_121.xy).xyz), vec3(clamp (tmpvar_121.z, 0.000000, 1.000000)))).xyz;
  vec4 c_i0;
  c_i0.xyz = (o_i0.Albedo * light.xyz).xyz;
  c_i0.w = float(vec4(o_i0.Alpha));
  col = c_i0;
  col.xyz = (c_i0.xyz + o_i0.Emission).xyz;
  gl_FragData[0] = col.xyzw;
}

