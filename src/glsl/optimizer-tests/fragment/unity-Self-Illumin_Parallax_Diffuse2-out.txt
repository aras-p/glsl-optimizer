varying vec4 xlv_FOG;
uniform vec4 unity_Ambient;
uniform sampler2D _ParallaxMap;
uniform float _Parallax;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
uniform sampler2D _Illum;
uniform vec4 _Color;
uniform sampler2D _BumpMap;
void main ()
{
  vec4 tmpvar_3;
  tmpvar_3 = gl_TexCoord[0].xyzw;
  vec4 col;
  vec4 light;
  vec2 tmpvar_55;
  tmpvar_55 = tmpvar_3.zw;
  vec2 _ret_val_i0;
  vec3 v;
  vec3 tmpvar_68;
  tmpvar_68 = normalize (gl_TexCoord[2].xyz);
  v = tmpvar_68;
  v.z = (vec3((tmpvar_68.z + 0.42))).z;
  _ret_val_i0 = (((texture2D (_ParallaxMap, tmpvar_55).w * _Parallax) - (_Parallax / 2.0)) * (v.xy / v.z));
  vec4 tmpvar_76;
  tmpvar_76 = (texture2D (_MainTex, (tmpvar_3.xy + _ret_val_i0)) * _Color);
  vec4 normal;
  normal.xy = ((texture2D (_BumpMap, (tmpvar_55 + _ret_val_i0)).wy * 2.0) - 1.0);
  normal.z = (vec3(sqrt (((1.0 - (normal.x * normal.x)) - (normal.y * normal.y))))).z;
  vec4 tmpvar_92;
  tmpvar_92 = -(log2 (texture2DProj (_LightBuffer, gl_TexCoord[3].xyzw)));
  light = tmpvar_92;
  light.xyz = (tmpvar_92.xyz + unity_Ambient.xyz).xyz;
  vec4 c_i0;
  c_i0.xyz = (tmpvar_76.xyz * light.xyz).xyz;
  c_i0.w = tmpvar_76.w;
  col = c_i0;
  col.xyz = (c_i0.xyz + (tmpvar_76.xyz * texture2D (_Illum, (gl_TexCoord[1].xy + _ret_val_i0)).w)).xyz;
  gl_FragData[0] = col.xyzw;
}

