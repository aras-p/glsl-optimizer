struct SurfaceOutput {
  vec3 Albedo;
  vec3 Normal;
  vec3 Emission;
  float Specular;
  float Gloss;
  float Alpha;
};
struct Input {
  vec2 uv_BumpMap;
  vec3 viewDir;
};
struct v2f_surf {
  vec4 pos;
  float fog;
  vec2 hip_pack0;
  vec3 lightDir;
  vec3 _LightCoord;
};
varying vec4 xlv_FOG;
uniform float _RimPower;
uniform vec4 _RimColor;
uniform sampler2D _MainTex;
uniform sampler2D _LightTextureB0;
uniform samplerCube _LightTexture0;
uniform vec4 _LightColor0;
uniform sampler2D _BumpMap;
void main ()
{
  v2f_surf xlt_IN;
  xlt_IN.pos = vec4(0.000000, 0.000000, 0.000000, 0.000000);
  xlt_IN.fog = xlv_FOG.x;
  xlt_IN.hip_pack0 = gl_TexCoord[0].xy;
  xlt_IN.lightDir = gl_TexCoord[1].xyz;
  xlt_IN._LightCoord = gl_TexCoord[2].xyz;
  v2f_surf IN;
  IN = xlt_IN;
  vec4 c;
  vec3 lightDir;
  SurfaceOutput o;
  Input surfIN;
  surfIN.uv_BumpMap = xlt_IN.hip_pack0.xy;
  o.Albedo = vec3(0.000000, 0.000000, 0.000000);
  o.Emission = vec3(0.000000, 0.000000, 0.000000);
  o.Specular = 0.000000;
  o.Alpha = 0.000000;
  o.Gloss = 0.000000;
  Input IN_i0;
  IN_i0 = surfIN;
  SurfaceOutput o_i0;
  o_i0 = o;
  vec4 tmpvar_50;
  tmpvar_50 = texture2D (_MainTex, IN_i0.uv_BumpMap);
  o_i0.Albedo = tmpvar_50.xyz;
  vec4 tmpvar_52;
  tmpvar_52 = texture2D (_BumpMap, IN_i0.uv_BumpMap);
  vec4 normal;
  normal.xy = ((tmpvar_52.wy * 2.000000) - 1.000000).xy;
  float tmpvar_56;
  tmpvar_56 = sqrt (((1.000000 - (normal.x * normal.x)) - (normal.y * normal.y)));
  normal.z = float(vec3(tmpvar_56));
  o_i0.Normal = normal.xyz;
  vec3 tmpvar_60;
  tmpvar_60 = normalize (IN_i0.viewDir);
  float tmpvar_61;
  tmpvar_61 = dot (tmpvar_60, o_i0.Normal);
  float tmpvar_64;
  tmpvar_64 = clamp (tmpvar_61, 0.000000, 1.000000);
  float tmpvar_66;
  tmpvar_66 = pow ((1.000000 - tmpvar_64), _RimPower);
  o_i0.Emission = (_RimColor.xyz * tmpvar_66);
  o = o_i0;
  lightDir = IN.lightDir;
  vec3 tmpvar_69;
  tmpvar_69 = normalize (lightDir);
  lightDir = tmpvar_69;
  float tmpvar_71;
  tmpvar_71 = dot (IN._LightCoord, IN._LightCoord);
  vec4 tmpvar_74;
  tmpvar_74 = texture2D (_LightTextureB0, vec2(tmpvar_71));
  vec4 tmpvar_75;
  tmpvar_75 = textureCube (_LightTexture0, IN._LightCoord);
  SurfaceOutput s;
  s = o;
  float atten;
  atten = (tmpvar_74.w * tmpvar_75.w);
  vec4 c_i0;
  float tmpvar_78;
  tmpvar_78 = dot (s.Normal, lightDir);
  float tmpvar_79;
  tmpvar_79 = max (0.000000, tmpvar_78);
  c_i0.xyz = ((s.Albedo * _LightColor0.xyz) * ((tmpvar_79 * atten) * 2.000000)).xyz;
  c_i0.w = float(vec4(s.Alpha));
  c = c_i0;
  c.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  gl_FragData[0] = c.xyzw;
}

