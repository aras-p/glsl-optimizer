varying vec4 xlv_FOG;
uniform sampler2D unity_LightmapInd;
uniform sampler2D unity_Lightmap;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
uniform sampler2D _Illum;
uniform vec4 _Color;
void main ()
{
  vec4 tmpvar_3;
  tmpvar_3 = gl_TexCoord[0].xyzw;
  vec3 tmpvar_7;
  tmpvar_7 = gl_TexCoord[2].xyz;
  vec4 col;
  vec4 light;
  vec4 tmpvar_37;
  tmpvar_37 = (texture2D (_MainTex, tmpvar_3.xy) * _Color);
  vec4 tmpvar_45;
  tmpvar_45 = -(log2 (texture2DProj (_LightBuffer, gl_TexCoord[1].xyzw)));
  light = tmpvar_45;
  light.xyz = (tmpvar_45.xyz + mix ((2.000000 * texture2D (unity_LightmapInd, tmpvar_7.xy).xyz), (2.000000 * texture2D (unity_Lightmap, tmpvar_7.xy).xyz), vec3(clamp (tmpvar_7.z, 0.000000, 1.000000)))).xyz;
  vec4 c_i0;
  c_i0.xyz = (tmpvar_37.xyz * light.xyz).xyz;
  c_i0.w = float(tmpvar_37.w);
  col = c_i0;
  col.xyz = (c_i0.xyz + (tmpvar_37.xyz * texture2D (_Illum, tmpvar_3.zw).w)).xyz;
  gl_FragData[0] = col.xyzw;
}

