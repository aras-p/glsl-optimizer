varying vec4 xlv_FOG;
uniform vec4 unity_Ambient;
uniform vec4 _SpecColor;
uniform float _Shininess;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
uniform sampler2D _Illum;
uniform vec4 _Color;
uniform sampler2D _BumpMap;
void main ()
{
  vec4 tmpvar_3;
  tmpvar_3 = gl_TexCoord[0].xyzw;
  vec4 col;
  vec4 light;
  vec2 tmpvar_89;
  vec4 tmpvar_44;
  tmpvar_44 = texture2D (_MainTex, tmpvar_3.xy);
  vec4 tmpvar_46;
  tmpvar_46 = (tmpvar_44 * _Color);
  vec4 normal;
  normal.xy = ((texture2D (_BumpMap, tmpvar_89).wy * 2.0) - 1.0);
  normal.z = (vec3(sqrt (((1.0 - (normal.x * normal.x)) - (normal.y * normal.y))))).z;
  vec4 tmpvar_64;
  tmpvar_64 = -(log2 (texture2DProj (_LightBuffer, gl_TexCoord[1].xyzw)));
  light = tmpvar_64;
  light.xyz = (tmpvar_64.xyz + unity_Ambient.xyz).xyz;
  vec4 c_i0;
  float tmpvar_68;
  tmpvar_68 = (light.w * tmpvar_44.w);
  c_i0.xyz = ((tmpvar_46.xyz * light.xyz) + ((light.xyz * _SpecColor.xyz) * tmpvar_68)).xyz;
  c_i0.w = (vec4((tmpvar_46.w + (tmpvar_68 * _SpecColor.w)))).w;
  col = c_i0;
  col.xyz = (c_i0.xyz + (tmpvar_46.xyz * texture2D (_Illum, tmpvar_3.zw).w)).xyz;
  gl_FragData[0] = col.xyzw;
}

