struct v2f {
  vec4 pos;
  vec2 uv;
  vec4 nz;
};
uniform sampler2D _MainTex;
uniform float _Cutoff;
void main ()
{
  v2f xlt_i;
  xlt_i.pos = vec4(0.000000, 0.000000, 0.000000, 0.000000);
  xlt_i.uv = gl_TexCoord[0].xy;
  xlt_i.nz = gl_TexCoord[1].xyzw;
  v2f i;
  i = xlt_i;
  vec4 tmpvar_40;
  tmpvar_40 = texture2D (_MainTex, i.uv);
  float x;
  x = (tmpvar_40.w - _Cutoff);
  if ((x < 0.000000)) {
    discard;
  };
  vec3 normal;
  normal = i.nz.xyz;
  vec4 enc;
  enc.xy = ((((normal.xy / (normal.z + 1.000000)) / 1.777700) * 0.500000) + 0.500000).xy;
  vec2 enc_i0_i1;
  enc_i0_i1 = (vec2(1.000000, 255.000000) * i.nz.w);
  vec2 tmpvar_56;
  tmpvar_56 = fract (enc_i0_i1);
  enc_i0_i1 = tmpvar_56;
  enc_i0_i1.x = (tmpvar_56.x - (tmpvar_56.y * 0.003922));
  enc.zw = vec2(enc_i0_i1.xy);
  gl_FragData[0] = enc.xyzw;
}

