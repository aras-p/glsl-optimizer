varying vec4 xlv_FOG;
uniform vec4 _SpecColor;
uniform float _Shininess;
uniform sampler2D _MainTex;
uniform sampler2D _LightTextureB0;
uniform samplerCube _LightTexture0;
uniform vec4 _LightColor0;
uniform float _Cutoff;
uniform vec4 _Color;
void main ()
{
  vec3 tmpvar_87;
  vec3 tmpvar_88;
  vec3 tmpvar_89;
  tmpvar_87 = gl_TexCoord[2].xyz;
  tmpvar_88 = gl_TexCoord[3].xyz;
  tmpvar_89 = gl_TexCoord[4].xyz;
  vec4 c;
  vec3 tmpvar_90;
  vec3 tmpvar_91;
  float tmpvar_93;
  float tmpvar_94;
  float tmpvar_95;
  vec4 tmpvar_44;
  tmpvar_44 = texture2D (_MainTex, gl_TexCoord[0].xy);
  float tmpvar_48;
  tmpvar_48 = (tmpvar_44.w * _Color.w);
  tmpvar_90 = (tmpvar_44.xyz * _Color.xyz);
  tmpvar_91 = gl_TexCoord[1].xyz;
  tmpvar_93 = _Shininess;
  tmpvar_94 = tmpvar_44.w;
  tmpvar_95 = tmpvar_48;
  float x;
  x = (tmpvar_48 - _Cutoff);
  if ((x < 0.000000)) {
    discard;
  };
  vec3 tmpvar_51;
  tmpvar_51 = normalize (tmpvar_87);
  float atten;
  atten = (texture2D (_LightTextureB0, vec2(dot (tmpvar_89, tmpvar_89))).w * textureCube (_LightTexture0, tmpvar_89).w);
  vec4 c_i0;
  float tmpvar_71;
  tmpvar_71 = (pow (max (0.000000, dot (tmpvar_91, normalize ((tmpvar_51 + normalize (tmpvar_88.xyz))))), (tmpvar_93 * 128.000000)) * tmpvar_94);
  c_i0.xyz = ((((tmpvar_90 * _LightColor0.xyz) * max (0.000000, dot (tmpvar_91, tmpvar_51))) + ((_LightColor0.xyz * _SpecColor.xyz) * tmpvar_71)) * (atten * 2.000000)).xyz;
  c_i0.w = float(vec4((tmpvar_95 + (((_LightColor0.w * _SpecColor.w) * tmpvar_71) * atten))));
  c = c_i0;
  c.w = float(vec4(tmpvar_95));
  gl_FragData[0] = c.xyzw;
}

