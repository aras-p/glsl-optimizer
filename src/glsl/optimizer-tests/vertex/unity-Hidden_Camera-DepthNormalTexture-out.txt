struct appdata {
  vec4 vertex;
  vec3 normal;
  vec4 color;
  vec4 texcoord;
};
uniform mat4 _TerrainEngineBendTree;
uniform vec4 _SquashPlaneNormal;
uniform float _SquashAmount;
uniform vec4 _Scale;
uniform vec4 _ProjectionParams;
void main ()
{
  appdata xlt_v;
  xlt_v.vertex = gl_Vertex.xyzw;
  xlt_v.normal = gl_Normal.xyz;
  xlt_v.color = gl_Color.xyzw;
  xlt_v.texcoord = gl_MultiTexCoord0.xyzw;
  appdata v;
  v = xlt_v;
  vec4 tmpvar_105;
  vec4 pos;
  pos = xlt_v.vertex;
  pos.xyz = (pos.xyz * _Scale.xyz).xyz;
  vec4 tmpvar_59;
  tmpvar_59.xyz = vec3(pos.xyz);
  tmpvar_59.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  vec3 tmpvar_62;
  tmpvar_62 = mix (pos.xyz, (_TerrainEngineBendTree * tmpvar_59).xyz, xlt_v.color.www);
  pos.xyz = tmpvar_62.xyz;
  vec4 pos_i0;
  pos_i0 = pos;
  vec3 planeNormal;
  vec3 projectedVertex;
  projectedVertex = pos.xyz;
  vec3 tmpvar_67;
  tmpvar_67.x = float(vec3(0.000000, 0.000000, 0.000000));
  tmpvar_67.y = float(_SquashPlaneNormal.w);
  tmpvar_67.z = float(vec3(0.000000, 0.000000, 0.000000));
  vec3 tmpvar_69;
  tmpvar_69 = _SquashPlaneNormal.xyz;
  planeNormal = tmpvar_69;
  float tmpvar_71;
  tmpvar_71 = dot (tmpvar_69, (tmpvar_67 - pos.xyz));
  vec3 tmpvar_72;
  tmpvar_72 = (projectedVertex + (tmpvar_71 * planeNormal));
  projectedVertex = tmpvar_72;
  vec3 tmpvar_74;
  tmpvar_74 = mix (tmpvar_72, pos_i0.xyz, vec3(_SquashAmount));
  vec4 tmpvar_75;
  tmpvar_75.xyz = vec3(tmpvar_74.xyz);
  tmpvar_75.w = float(vec4(1.000000, 1.000000, 1.000000, 1.000000));
  pos_i0 = tmpvar_75;
  pos = tmpvar_75;
  v.vertex = tmpvar_75;
  mat3 tmpvar_85;
  tmpvar_85[0] = gl_ModelViewMatrixInverseTranspose[0].xyz;
  tmpvar_85[1] = gl_ModelViewMatrixInverseTranspose[1].xyz;
  tmpvar_85[2] = gl_ModelViewMatrixInverseTranspose[2].xyz;
  tmpvar_105.xyz = (tmpvar_85 * v.normal).xyz;
  tmpvar_105.w = float(vec4(-(((gl_ModelViewMatrix * v.vertex).z * _ProjectionParams.w))));
  gl_Position = (gl_ModelViewProjectionMatrix * v.vertex).xyzw;
  vec4 tmpvar_13;
  tmpvar_13.xy = vec2(v.texcoord.xy);
  tmpvar_13.z = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_13.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  gl_TexCoord[0] = tmpvar_13;
  gl_TexCoord[1] = tmpvar_105.xyzw;
}

