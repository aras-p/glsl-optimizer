struct v2f_surf {
  vec4 pos;
  float fog;
  vec4 hip_pack0;
  vec3 viewDir;
  vec3 lightDir;
  vec3 vlight;
};
struct appdata_full {
  vec4 vertex;
  vec4 tangent;
  vec3 normal;
  vec4 texcoord;
  vec4 texcoord1;
  vec4 color;
};
uniform vec4 _BumpMap_ST;
uniform vec4 _MainTex_ST;
uniform mat4 _Object2World;
uniform mat4 _World2Object;
uniform vec3 _WorldSpaceCameraPos;
uniform vec4 _WorldSpaceLightPos0;
uniform vec4 unity_SHAb;
uniform vec4 unity_SHAg;
uniform vec4 unity_SHAr;
uniform vec4 unity_SHBb;
uniform vec4 unity_SHBg;
uniform vec4 unity_SHBr;
uniform vec4 unity_SHC;
uniform vec4 unity_Scale;
attribute vec4 TANGENT;
varying vec4 xlv_FOG;
void main ()
{
  appdata_full xlt_v;
  xlt_v.vertex = gl_Vertex.xyzw;
  xlt_v.tangent = TANGENT.xyzw;
  xlt_v.normal = gl_Normal.xyz;
  xlt_v.texcoord = gl_MultiTexCoord0.xyzw;
  xlt_v.texcoord1 = gl_MultiTexCoord1.xyzw;
  xlt_v.color = gl_Color.xyzw;
  appdata_full v;
  v = xlt_v;
  v2f_surf o;
  vec3 worldN;
  vec4 tmpvar_59;
  tmpvar_59 = (gl_ModelViewProjectionMatrix * xlt_v.vertex);
  o.pos = tmpvar_59;
  o.fog = tmpvar_59.z;
  o.hip_pack0.xy = ((xlt_v.texcoord.xy * _MainTex_ST.xy) + _MainTex_ST.zw).xy;
  o.hip_pack0.zw = vec2(((xlt_v.texcoord.xy * _BumpMap_ST.xy) + _BumpMap_ST.zw).xxxy);
  mat3 tmpvar_68;
  tmpvar_68[0] = _Object2World[0].xyz;
  tmpvar_68[1] = _Object2World[1].xyz;
  tmpvar_68[2] = _Object2World[2].xyz;
  worldN = (tmpvar_68 * (xlt_v.normal * unity_Scale.w));
  vec3 tmpvar_73;
  tmpvar_73 = cross (v.normal, v.tangent.xyz);
  vec3 tmpvar_74;
  tmpvar_74 = (tmpvar_73 * v.tangent.w);
  mat3 tmpvar_75;
  tmpvar_75[0].x = float(v.tangent.xxx);
  tmpvar_75[0].y = float(tmpvar_74.xxx);
  tmpvar_75[0].z = float(v.normal.xxx);
  tmpvar_75[1].x = float(v.tangent.yyy);
  tmpvar_75[1].y = float(tmpvar_74.yyy);
  tmpvar_75[1].z = float(v.normal.yyy);
  tmpvar_75[2].x = float(v.tangent.zzz);
  tmpvar_75[2].y = float(tmpvar_74.zzz);
  tmpvar_75[2].z = float(v.normal.zzz);
  o.lightDir = (tmpvar_75 * (_World2Object * _WorldSpaceLightPos0).xyz);
  vec4 tmpvar_92;
  tmpvar_92.xyz = vec3(_WorldSpaceCameraPos.xyzx);
  tmpvar_92.w = float(vec4 (1.000000, 1.000000, 1.000000, 1.000000));
  o.viewDir = (tmpvar_75 * (((_World2Object * tmpvar_92).xyz * unity_Scale.w) - v.vertex.xyz));
  vec4 tmpvar_95;
  tmpvar_95.xyz = vec3(worldN.xyzx);
  tmpvar_95.w = float(vec4 (1.000000, 1.000000, 1.000000, 1.000000));
  vec4 normal;
  normal = tmpvar_95;
  vec3 x1;
  vec4 vB;
  vec3 x2;
  float tmpvar_98;
  tmpvar_98 = dot (unity_SHAr, normal);
  x1.x = tmpvar_98;
  float tmpvar_100;
  tmpvar_100 = dot (unity_SHAg, normal);
  x1.y = float(vec2(tmpvar_100));
  float tmpvar_102;
  tmpvar_102 = dot (unity_SHAb, normal);
  x1.z = float(vec3(tmpvar_102));
  vB = (normal.xyzz * normal.yzzx);
  float tmpvar_105;
  tmpvar_105 = dot (unity_SHBr, vB);
  x2.x = tmpvar_105;
  float tmpvar_107;
  tmpvar_107 = dot (unity_SHBg, vB);
  x2.y = float(vec2(tmpvar_107));
  float tmpvar_109;
  tmpvar_109 = dot (unity_SHBb, vB);
  x2.z = float(vec3(tmpvar_109));
  o.vlight = ((x1 + x2) + (unity_SHC.xyz * ((normal.x * normal.x) - (normal.y * normal.y))));
  gl_Position = o.pos.xyzw;
  vec4 tmpvar_17;
  tmpvar_17.x = float(vec4(o.fog));
  tmpvar_17.y = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_17.z = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_17.w = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  xlv_FOG = tmpvar_17;
  gl_TexCoord[0] = o.hip_pack0.xyzw;
  vec4 tmpvar_21;
  tmpvar_21.xyz = vec3(o.viewDir.xyzx);
  tmpvar_21.w = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  gl_TexCoord[1] = tmpvar_21;
  vec4 tmpvar_23;
  tmpvar_23.xyz = vec3(o.lightDir.xyzx);
  tmpvar_23.w = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  gl_TexCoord[2] = tmpvar_23;
  vec4 tmpvar_25;
  tmpvar_25.xyz = vec3(o.vlight.xyzx);
  tmpvar_25.w = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  gl_TexCoord[3] = tmpvar_25;
}

