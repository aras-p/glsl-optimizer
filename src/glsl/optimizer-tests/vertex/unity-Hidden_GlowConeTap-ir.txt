struct appdata_img {
  vec4 vertex;
  vec2 texcoord;
};
struct v2f {
  vec4 pos;
  vec4 uv[2];
};
uniform vec4 _BlurOffsets;
uniform vec4 _MainTex_TexelSize;
vec2 MultiplyUV (
  in mat4 mat,
  in vec2 inUV
);

vec2 MultiplyUV (
  in mat4 mat,
  in vec2 inUV
)
{
  vec4 temp;
  vec4 tmpvar_1;
  tmpvar_1.x = float(vec4(inUV.x));
  tmpvar_1.y = float(vec4(inUV.y));
  tmpvar_1.z = float(vec4(0.000000));
  tmpvar_1.w = float(vec4(0.000000));
  vec4 tmpvar_2;
  tmpvar_2 = tmpvar_1;
  temp = tmpvar_2;
  vec4 tmpvar_3;
  tmpvar_3 = (mat * temp);
  temp = tmpvar_3;
  return temp.xy;
}

v2f vert (
  in appdata_img v
);

v2f vert (
  in appdata_img v
)
{
  float offX;
  float offY;
  v2f o;
  vec2 uv;
  float tmpvar_1;
  tmpvar_1 = (_MainTex_TexelSize.x * _BlurOffsets.x);
  offX = tmpvar_1;
  float tmpvar_2;
  tmpvar_2 = (_MainTex_TexelSize.y * _BlurOffsets.y);
  offY = tmpvar_2;
  vec4 tmpvar_3;
  tmpvar_3 = (gl_ModelViewProjectionMatrix * v.vertex);
  o.pos = tmpvar_3;
  vec2 tmpvar_4;
  tmpvar_4.x = float(vec2(offX));
  tmpvar_4.y = float(vec2(offY));
  vec2 tmpvar_5;
  tmpvar_5 = MultiplyUV (gl_TextureMatrix[0], (v.texcoord.xy - tmpvar_4));
  vec2 tmpvar_6;
  tmpvar_6 = tmpvar_5;
  uv = tmpvar_6;
  vec2 tmpvar_7;
  tmpvar_7.x = float(vec2(offX));
  tmpvar_7.y = float(vec2(offY));
  vec2 tmpvar_8;
  tmpvar_8 = (uv + tmpvar_7);
  o.uv[0].xy = tmpvar_8.xy;
  vec2 tmpvar_9;
  tmpvar_9.x = float(vec2(-(offX)));
  tmpvar_9.y = float(vec2(offY));
  vec2 tmpvar_10;
  tmpvar_10 = (uv + tmpvar_9);
  o.uv[0].zw = vec2(tmpvar_10.xy);
  vec2 tmpvar_11;
  tmpvar_11.x = float(vec2(offX));
  tmpvar_11.y = float(vec2(-(offY)));
  vec2 tmpvar_12;
  tmpvar_12 = (uv + tmpvar_11);
  o.uv[1].xy = tmpvar_12.xy;
  vec2 tmpvar_13;
  tmpvar_13.x = float(vec2(-(offX)));
  tmpvar_13.y = float(vec2(-(offY)));
  vec2 tmpvar_14;
  tmpvar_14 = (uv + tmpvar_13);
  o.uv[1].zw = vec2(tmpvar_14.xy);
  return o;
}

void main ()
{
  v2f xl_retval;
  appdata_img xlt_v;
  vec4 tmpvar_1;
  tmpvar_1 = gl_Vertex.xyzw;
  vec4 tmpvar_2;
  tmpvar_2 = tmpvar_1;
  xlt_v.vertex = tmpvar_2;
  vec2 tmpvar_3;
  tmpvar_3 = gl_MultiTexCoord0.xy;
  vec2 tmpvar_4;
  tmpvar_4 = tmpvar_3;
  xlt_v.texcoord = tmpvar_4;
  v2f tmpvar_5;
  tmpvar_5 = vert (xlt_v);
  v2f tmpvar_6;
  tmpvar_6 = tmpvar_5;
  xl_retval = tmpvar_6;
  vec4 tmpvar_7;
  tmpvar_7 = xl_retval.pos.xyzw;
  vec4 tmpvar_8;
  tmpvar_8 = tmpvar_7;
  gl_Position = tmpvar_8;
  vec4 tmpvar_9;
  tmpvar_9 = xl_retval.uv[0].xyzw;
  vec4 tmpvar_10;
  tmpvar_10 = tmpvar_9;
  gl_TexCoord[0] = tmpvar_10;
  vec4 tmpvar_11;
  tmpvar_11 = xl_retval.uv[1].xyzw;
  vec4 tmpvar_12;
  tmpvar_12 = tmpvar_11;
  gl_TexCoord[1] = tmpvar_12;
}

