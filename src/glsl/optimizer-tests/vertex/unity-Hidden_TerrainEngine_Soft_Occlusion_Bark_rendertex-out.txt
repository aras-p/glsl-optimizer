struct appdata_tree {
  vec4 vertex;
  vec4 tangent;
  vec3 normal;
  vec4 color;
  vec4 texcoord;
};
varying vec4 xlv_FOG;
attribute vec4 TANGENT;
uniform vec3 _TerrainTreeLightDirections[4];
uniform vec4 _TerrainTreeLightColors[4];
uniform mat4 _TerrainEngineBendTree;
uniform vec4 _SquashPlaneNormal;
uniform float _SquashAmount;
uniform vec4 _Scale;
uniform vec4 _Color;
uniform float _AO;
void main ()
{
  appdata_tree xlt_v;
  xlt_v.vertex = gl_Vertex.xyzw;
  xlt_v.tangent = TANGENT.xyzw;
  xlt_v.normal = gl_Normal.xyz;
  xlt_v.color = gl_Color.xyzw;
  xlt_v.texcoord = gl_MultiTexCoord0.xyzw;
  appdata_tree v;
  v = xlt_v;
  int i;
  vec4 lightColor;
  vec4 lightDir;
  vec4 tmpvar_110;
  float tmpvar_111;
  vec4 tmpvar_112;
  vec4 tmpvar_113;
  i = 0;
  vec4 pos;
  pos = xlt_v.vertex;
  pos.xyz = (pos.xyz * _Scale.xyz).xyz;
  vec4 tmpvar_57;
  tmpvar_57.xyz = vec3(pos.xyz);
  tmpvar_57.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  pos.xyz = mix (pos.xyz, (_TerrainEngineBendTree * tmpvar_57).xyz, xlt_v.color.www).xyz;
  vec3 tmpvar_65;
  tmpvar_65.x = float(vec3(0.000000, 0.000000, 0.000000));
  tmpvar_65.y = float(_SquashPlaneNormal.w);
  tmpvar_65.z = float(vec3(0.000000, 0.000000, 0.000000));
  vec3 tmpvar_67;
  tmpvar_67 = _SquashPlaneNormal.xyz;
  vec4 tmpvar_73;
  tmpvar_73.xyz = vec3(mix ((pos.xyz + (dot (tmpvar_67, (tmpvar_65 - pos.xyz)) * tmpvar_67)), pos.xyz, vec3(_SquashAmount)).xyz);
  tmpvar_73.w = float(vec4(1.000000, 1.000000, 1.000000, 1.000000));
  pos = tmpvar_73;
  v.vertex = tmpvar_73;
  tmpvar_110 = (gl_ModelViewProjectionMatrix * v.vertex);
  tmpvar_111 = tmpvar_110.z;
  tmpvar_112 = v.texcoord;
  lightDir.w = float(vec4(_AO));
  lightColor = gl_LightModel.ambient;
  while (true) {
    if (!((i < 4))) {
      break;
    };
    lightDir.xyz = _TerrainTreeLightDirections[i].xyz;
    lightColor = (lightColor + (_TerrainTreeLightColors[i] * max (0.000000, dot (lightDir.xyz, v.normal))));
    i = (i + 1);
  };
  lightColor.w = float(vec4(1.000000, 1.000000, 1.000000, 1.000000));
  tmpvar_113 = (lightColor * _Color);
  tmpvar_113.w = float(vec4(1.000000, 1.000000, 1.000000, 1.000000));
  gl_Position = tmpvar_110.xyzw;
  vec4 tmpvar_15;
  tmpvar_15.x = float(vec4(tmpvar_111));
  tmpvar_15.y = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_15.z = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_15.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  xlv_FOG = tmpvar_15;
  gl_TexCoord[0] = tmpvar_112.xyzw;
  gl_FrontColor = tmpvar_113.xyzw;
}

