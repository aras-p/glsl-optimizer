struct v2f {
  vec4 pos;
  float fog;
  vec4 color;
  vec2 uv;
};
struct appdata_tree_billboard {
  vec4 vertex;
  vec4 color;
  vec4 texcoord;
  vec2 texcoord1;
};
uniform vec4 _TreeBillboardCameraFront;
uniform vec4 _TreeBillboardCameraPos;
uniform vec3 _TreeBillboardCameraRight;
uniform vec4 _TreeBillboardCameraUp;
uniform vec4 _TreeBillboardDistances;
varying vec4 xlv_FOG;
void main ()
{
  appdata_tree_billboard xlt_v;
  xlt_v.vertex = gl_Vertex.xyzw;
  xlt_v.color = gl_Color.xyzw;
  xlt_v.texcoord = gl_MultiTexCoord0.xyzw;
  xlt_v.texcoord1 = gl_MultiTexCoord1.xy;
  appdata_tree_billboard v;
  v = xlt_v;
  v2f o;
  vec4 pos;
  pos = xlt_v.vertex;
  vec2 offset;
  offset = xlt_v.texcoord1.xy;
  float offsetz;
  offsetz = xlt_v.texcoord.y;
  vec3 treePos;
  float radius;
  float billboardModeFactor;
  float billboardRootOffsetFactor;
  float absRadius;
  treePos = (pos.xyz - _TreeBillboardCameraPos.xyz);
  float tmpvar_37;
  tmpvar_37 = dot (treePos, treePos);
  if ((tmpvar_37 > _TreeBillboardDistances.x)) {
    offset = vec2 (0.000000, 0.000000);
  };
  pos.xyz = (pos.xyz + (_TreeBillboardCameraRight.xyz * offset.x)).xyz;
  float tmpvar_42;
  tmpvar_42 = mix (offset.y, offsetz, _TreeBillboardCameraPos.w);
  radius = tmpvar_42;
  billboardModeFactor = _TreeBillboardCameraUp.w;
  billboardRootOffsetFactor = _TreeBillboardCameraFront.w;
  float tmpvar_46;
  tmpvar_46 = abs (radius);
  absRadius = tmpvar_46;
  float tmpvar_48;
  tmpvar_48 = max (0.000000, radius);
  vec3 tmpvar_50;
  tmpvar_50 = mix (((((_TreeBillboardCameraUp.xyz * tmpvar_48) * 2.000000) - (vec3 (0.000000, 1.000000, 0.000000) * absRadius)) - ((_TreeBillboardCameraUp.xyz * absRadius) * billboardRootOffsetFactor)), (_TreeBillboardCameraUp.xyz * radius), vec3(billboardModeFactor));
  pos.xyz = (pos.xyz + tmpvar_50).xyz;
  v.vertex = pos;
  o.pos = (gl_ModelViewProjectionMatrix * v.vertex);
  o.fog = o.pos.z;
  o.uv.x = v.texcoord.x;
  o.uv.y = float(vec2(float((v.texcoord.y > 0.000000))));
  o.color = v.color;
  gl_Position = o.pos.xyzw;
  vec4 tmpvar_13;
  tmpvar_13.x = float(vec4(o.fog));
  tmpvar_13.y = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_13.z = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_13.w = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  xlv_FOG = tmpvar_13;
  gl_FrontColor = o.color.xyzw;
  vec4 tmpvar_17;
  tmpvar_17.xy = vec2(o.uv.xyxx);
  tmpvar_17.z = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_17.w = float(vec4 (0.000000, 0.000000, 0.000000, 0.000000));
  gl_TexCoord[0] = tmpvar_17;
}

