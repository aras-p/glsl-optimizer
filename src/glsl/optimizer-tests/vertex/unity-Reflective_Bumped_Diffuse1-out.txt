struct v2f_surf {
  vec4 pos;
  float fog;
  vec4 hip_pack0;
  vec4 hip_screen;
  vec4 TtoW0;
  vec4 TtoW1;
  vec4 TtoW2;
  vec3 hip_lmapFade;
};
struct appdata_full {
  vec4 vertex;
  vec4 tangent;
  vec3 normal;
  vec4 texcoord;
  vec4 texcoord1;
  vec4 color;
};
varying vec4 xlv_FOG;
attribute vec4 TANGENT;
uniform vec4 unity_Scale;
uniform vec4 unity_LightmapST;
uniform vec4 unity_LightmapFade;
uniform vec3 _WorldSpaceCameraPos;
uniform mat4 _World2Object;
uniform vec4 _ProjectionParams;
uniform mat4 _Object2World;
uniform vec4 _MainTex_ST;
uniform vec4 _BumpMap_ST;
void main ()
{
  appdata_full xlt_v;
  xlt_v.vertex = gl_Vertex.xyzw;
  xlt_v.tangent = TANGENT.xyzw;
  xlt_v.normal = gl_Normal.xyz;
  xlt_v.texcoord = gl_MultiTexCoord0.xyzw;
  xlt_v.texcoord1 = gl_MultiTexCoord1.xyzw;
  xlt_v.color = gl_Color.xyzw;
  appdata_full v;
  v = xlt_v;
  vec3 worldRefl;
  v2f_surf o;
  vec4 tmpvar_50;
  tmpvar_50 = (gl_ModelViewProjectionMatrix * xlt_v.vertex);
  o.pos = tmpvar_50;
  o.fog = tmpvar_50.z;
  o.hip_pack0.xy = ((xlt_v.texcoord.xy * _MainTex_ST.xy) + _MainTex_ST.zw).xy;
  o.hip_pack0.zw = vec2(((xlt_v.texcoord.xy * _BumpMap_ST.xy) + _BumpMap_ST.zw).xy);
  vec4 tmpvar_56;
  tmpvar_56.xyz = vec3(_WorldSpaceCameraPos.xyz);
  tmpvar_56.w = float(vec4(1.000000, 1.000000, 1.000000, 1.000000));
  mat3 tmpvar_64;
  tmpvar_64[0] = _Object2World[0].xyz;
  tmpvar_64[1] = _Object2World[1].xyz;
  tmpvar_64[2] = _Object2World[2].xyz;
  worldRefl = (tmpvar_64 * -((((_World2Object * tmpvar_56).xyz * unity_Scale.w) - xlt_v.vertex.xyz)));
  vec3 tmpvar_69;
  tmpvar_69 = cross (v.normal, v.tangent.xyz);
  vec3 tmpvar_70;
  tmpvar_70 = (tmpvar_69 * v.tangent.w);
  mat3 tmpvar_71;
  tmpvar_71[0].x = float(v.tangent.x);
  tmpvar_71[0].y = float(tmpvar_70.x);
  tmpvar_71[0].z = float(v.normal.x);
  tmpvar_71[1].x = float(v.tangent.y);
  tmpvar_71[1].y = float(tmpvar_70.y);
  tmpvar_71[1].z = float(v.normal.y);
  tmpvar_71[2].x = float(v.tangent.z);
  tmpvar_71[2].y = float(tmpvar_70.z);
  tmpvar_71[2].z = float(v.normal.z);
  vec4 tmpvar_82;
  tmpvar_82.xyz = vec3((tmpvar_71 * _Object2World[0].xyz).xyz);
  tmpvar_82.w = float(worldRefl.x);
  o.TtoW0 = (tmpvar_82 * unity_Scale.w);
  vec4 tmpvar_84;
  tmpvar_84.xyz = vec3((tmpvar_71 * _Object2World[1].xyz).xyz);
  tmpvar_84.w = float(worldRefl.y);
  o.TtoW1 = (tmpvar_84 * unity_Scale.w);
  vec4 tmpvar_86;
  tmpvar_86.xyz = vec3((tmpvar_71 * _Object2World[2].xyz).xyz);
  tmpvar_86.w = float(worldRefl.z);
  o.TtoW2 = (tmpvar_86 * unity_Scale.w);
  vec4 pos_i0;
  pos_i0 = o.pos;
  vec4 o_i0;
  vec4 tmpvar_90;
  tmpvar_90 = (pos_i0 * 0.500000);
  o_i0 = tmpvar_90;
  vec2 tmpvar_91;
  tmpvar_91.x = float(tmpvar_90.x);
  tmpvar_91.y = float(vec2((tmpvar_90.y * _ProjectionParams.x)));
  o_i0.xy = (tmpvar_91 + tmpvar_90.w).xy;
  o_i0.zw = vec2(pos_i0.zw);
  o.hip_screen = o_i0;
  o.hip_lmapFade.xy = ((v.texcoord1.xy * unity_LightmapST.xy) + unity_LightmapST.zw).xy;
  o.hip_lmapFade.z = float(vec3(((-((gl_ModelViewMatrix * v.vertex).z) * unity_LightmapFade.z) + unity_LightmapFade.w)));
  gl_Position = o.pos.xyzw;
  vec4 tmpvar_17;
  tmpvar_17.x = float(vec4(o.fog));
  tmpvar_17.y = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_17.z = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_17.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  xlv_FOG = tmpvar_17;
  gl_TexCoord[0] = o.hip_pack0.xyzw;
  gl_TexCoord[1] = o.hip_screen.xyzw;
  gl_TexCoord[2] = o.TtoW0.xyzw;
  gl_TexCoord[3] = o.TtoW1.xyzw;
  gl_TexCoord[4] = o.TtoW2.xyzw;
  vec4 tmpvar_29;
  tmpvar_29.xyz = vec3(o.hip_lmapFade.xyz);
  tmpvar_29.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  gl_TexCoord[5] = tmpvar_29;
}

