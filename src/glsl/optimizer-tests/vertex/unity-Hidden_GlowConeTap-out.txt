struct appdata_img {
  vec4 vertex;
  vec2 texcoord;
};
struct v2f {
  vec4 pos;
  vec4 uv[2];
};
uniform vec4 _BlurOffsets;
uniform vec4 _MainTex_TexelSize;
void main ()
{
  appdata_img xlt_v;
  xlt_v.vertex = gl_Vertex.xyzw;
  xlt_v.texcoord = gl_MultiTexCoord0.xy;
  v2f o;
  float tmpvar_18;
  tmpvar_18 = (_MainTex_TexelSize.x * _BlurOffsets.x);
  float tmpvar_19;
  tmpvar_19 = (_MainTex_TexelSize.y * _BlurOffsets.y);
  o.pos = (gl_ModelViewProjectionMatrix * xlt_v.vertex);
  vec2 tmpvar_21;
  tmpvar_21.x = float(vec2(tmpvar_18));
  tmpvar_21.y = float(vec2(tmpvar_19));
  vec2 __retval_i0;
  vec2 inUV;
  inUV = (xlt_v.texcoord.xy - tmpvar_21);
  vec4 tmpvar_24;
  tmpvar_24.x = float(inUV.xxxx);
  tmpvar_24.y = float(inUV.yyyy);
  tmpvar_24.z = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_24.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  __retval_i0 = (gl_TextureMatrix[0] * tmpvar_24).xy;
  vec2 tmpvar_28;
  tmpvar_28.x = float(vec2(tmpvar_18));
  tmpvar_28.y = float(vec2(tmpvar_19));
  o.uv[0].xy = (__retval_i0 + tmpvar_28).xy;
  vec2 tmpvar_30;
  tmpvar_30.x = float(vec2(-(tmpvar_18)));
  tmpvar_30.y = float(vec2(tmpvar_19));
  o.uv[0].zw = vec2((__retval_i0 + tmpvar_30).xxxy);
  vec2 tmpvar_32;
  tmpvar_32.x = float(vec2(tmpvar_18));
  tmpvar_32.y = float(vec2(-(tmpvar_19)));
  o.uv[1].xy = (__retval_i0 + tmpvar_32).xy;
  vec2 tmpvar_34;
  tmpvar_34.x = float(vec2(-(tmpvar_18)));
  tmpvar_34.y = float(vec2(-(tmpvar_19)));
  o.uv[1].zw = vec2((__retval_i0 + tmpvar_34).xxxy);
  gl_Position = o.pos.xyzw;
  gl_TexCoord[0] = o.uv[0].xyzw;
  gl_TexCoord[1] = o.uv[1].xyzw;
}

