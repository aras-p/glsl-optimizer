uniform vec4 _MainTex_TexelSize;
uniform vec4 _BlurOffsets;
void main ()
{
  vec4 tmpvar_49[2];
  float tmpvar_18;
  tmpvar_18 = (_MainTex_TexelSize.x * _BlurOffsets.x);
  float tmpvar_19;
  tmpvar_19 = (_MainTex_TexelSize.y * _BlurOffsets.y);
  vec2 tmpvar_21;
  tmpvar_21.x = float(vec2(tmpvar_18));
  tmpvar_21.y = float(vec2(tmpvar_19));
  vec2 _ret_val_i0;
  vec2 inUV;
  inUV = (gl_MultiTexCoord0.xy - tmpvar_21);
  vec4 tmpvar_24;
  tmpvar_24.x = float(inUV.x);
  tmpvar_24.y = float(inUV.y);
  tmpvar_24.z = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_24.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  _ret_val_i0 = (gl_TextureMatrix[0] * tmpvar_24).xy;
  vec2 tmpvar_28;
  tmpvar_28.x = float(vec2(tmpvar_18));
  tmpvar_28.y = float(vec2(tmpvar_19));
  tmpvar_49[0].xy = (_ret_val_i0 + tmpvar_28).xy;
  vec2 tmpvar_30;
  tmpvar_30.x = float(vec2(-(tmpvar_18)));
  tmpvar_30.y = float(vec2(tmpvar_19));
  tmpvar_49[0].zw = vec2((_ret_val_i0 + tmpvar_30).xy);
  vec2 tmpvar_32;
  tmpvar_32.x = float(vec2(tmpvar_18));
  tmpvar_32.y = float(vec2(-(tmpvar_19)));
  tmpvar_49[1].xy = (_ret_val_i0 + tmpvar_32).xy;
  vec2 tmpvar_34;
  tmpvar_34.x = float(vec2(-(tmpvar_18)));
  tmpvar_34.y = float(vec2(-(tmpvar_19)));
  tmpvar_49[1].zw = vec2((_ret_val_i0 + tmpvar_34).xy);
  gl_Position = (gl_ModelViewProjectionMatrix * gl_Vertex.xyzw).xyzw;
  gl_TexCoord[0] = tmpvar_49[0].xyzw;
  gl_TexCoord[1] = tmpvar_49[1].xyzw;
}

