varying vec4 texcoord;
void main ()
{
  gl_Position.zw = vec2((gl_Vertex.xy * 2.000000).xy);
  gl_Position.xy = gl_Vertex.xy;
  texcoord.x = gl_Vertex.z;
  texcoord.yz = vec2(gl_Vertex.zw);
  texcoord.w = float(gl_Vertex.x);
}

