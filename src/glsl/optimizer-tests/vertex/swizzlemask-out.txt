varying vec4 texcoord;
void main ()
{
  gl_Position.zw = vec2((gl_Vertex.xy * 2.000000).xxxy);
  gl_Position.xy = gl_Vertex.xy;
  texcoord.x = gl_Vertex.z;
  texcoord.yz = vec2(gl_Vertex.zzw);
  texcoord.w = float(gl_Vertex.xxxx);
}

