struct appdata_full {
  vec4 vertex;
  vec4 tangent;
  vec3 normal;
  vec4 texcoord;
  vec4 texcoord1;
  vec4 color;
};
varying vec4 xlv_FOG;
attribute vec4 TANGENT;
uniform vec4 unity_Scale;
uniform vec3 _WorldSpaceCameraPos;
uniform mat4 _World2Object;
uniform vec4 _ProjectionParams;
uniform mat4 _Object2World;
uniform vec4 _MainTex_ST;
uniform vec4 _BumpMap_ST;
void main ()
{
  appdata_full xlt_v;
  xlt_v.vertex = gl_Vertex.xyzw;
  xlt_v.tangent = TANGENT.xyzw;
  xlt_v.normal = gl_Normal.xyz;
  xlt_v.texcoord = gl_MultiTexCoord0.xyzw;
  xlt_v.texcoord1 = gl_MultiTexCoord1.xyzw;
  xlt_v.color = gl_Color.xyzw;
  appdata_full v;
  v = xlt_v;
  vec3 worldRefl;
  vec4 tmpvar_135;
  float tmpvar_136;
  vec4 tmpvar_137;
  vec4 tmpvar_53;
  tmpvar_53 = (gl_ModelViewProjectionMatrix * xlt_v.vertex);
  tmpvar_135 = tmpvar_53;
  tmpvar_136 = tmpvar_53.z;
  tmpvar_137.xy = ((xlt_v.texcoord.xy * _MainTex_ST.xy) + _MainTex_ST.zw).xy;
  tmpvar_137.zw = vec2(((xlt_v.texcoord.xy * _BumpMap_ST.xy) + _BumpMap_ST.zw).xy);
  vec4 tmpvar_59;
  tmpvar_59.xyz = vec3(_WorldSpaceCameraPos.xyz);
  tmpvar_59.w = float(vec4(1.000000, 1.000000, 1.000000, 1.000000));
  mat3 tmpvar_67;
  tmpvar_67[0] = _Object2World[0].xyz;
  tmpvar_67[1] = _Object2World[1].xyz;
  tmpvar_67[2] = _Object2World[2].xyz;
  worldRefl = (tmpvar_67 * -((((_World2Object * tmpvar_59).xyz * unity_Scale.w) - xlt_v.vertex.xyz)));
  vec3 tmpvar_72;
  tmpvar_72 = cross (v.normal, v.tangent.xyz);
  vec3 tmpvar_73;
  tmpvar_73 = (tmpvar_72 * v.tangent.w);
  mat3 tmpvar_74;
  tmpvar_74[0].x = float(v.tangent.x);
  tmpvar_74[0].y = float(tmpvar_73.x);
  tmpvar_74[0].z = float(v.normal.x);
  tmpvar_74[1].x = float(v.tangent.y);
  tmpvar_74[1].y = float(tmpvar_73.y);
  tmpvar_74[1].z = float(v.normal.y);
  tmpvar_74[2].x = float(v.tangent.z);
  tmpvar_74[2].y = float(tmpvar_73.z);
  tmpvar_74[2].z = float(v.normal.z);
  vec4 tmpvar_85;
  tmpvar_85.xyz = vec3((tmpvar_74 * _Object2World[0].xyz).xyz);
  tmpvar_85.w = float(worldRefl.x);
  vec4 tmpvar_87;
  tmpvar_87.xyz = vec3((tmpvar_74 * _Object2World[1].xyz).xyz);
  tmpvar_87.w = float(worldRefl.y);
  vec4 tmpvar_89;
  tmpvar_89.xyz = vec3((tmpvar_74 * _Object2World[2].xyz).xyz);
  tmpvar_89.w = float(worldRefl.z);
  vec4 o_i0;
  vec4 tmpvar_93;
  tmpvar_93 = (tmpvar_135 * 0.500000);
  o_i0 = tmpvar_93;
  vec2 tmpvar_94;
  tmpvar_94.x = float(tmpvar_93.x);
  tmpvar_94.y = float(vec2((tmpvar_93.y * _ProjectionParams.x)));
  o_i0.xy = (tmpvar_94 + tmpvar_93.w).xy;
  o_i0.zw = vec2(tmpvar_135.zw);
  vec4 tmpvar_100;
  tmpvar_100.xyz = vec3(_WorldSpaceCameraPos.xyz);
  tmpvar_100.w = float(vec4(1.000000, 1.000000, 1.000000, 1.000000));
  gl_Position = tmpvar_135.xyzw;
  vec4 tmpvar_17;
  tmpvar_17.x = float(vec4(tmpvar_136));
  tmpvar_17.y = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_17.z = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  tmpvar_17.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  xlv_FOG = tmpvar_17;
  gl_TexCoord[0] = tmpvar_137.xyzw;
  vec4 tmpvar_21;
  tmpvar_21.xyz = vec3((tmpvar_74 * (((_World2Object * tmpvar_100).xyz * unity_Scale.w) - v.vertex.xyz)).xyz);
  tmpvar_21.w = float(vec4(0.000000, 0.000000, 0.000000, 0.000000));
  gl_TexCoord[1] = tmpvar_21;
  gl_TexCoord[2] = o_i0.xyzw;
  gl_TexCoord[3] = (tmpvar_85 * unity_Scale.w).xyzw;
  gl_TexCoord[4] = (tmpvar_87 * unity_Scale.w).xyzw;
  gl_TexCoord[5] = (tmpvar_89 * unity_Scale.w).xyzw;
}

