Import('*')

if env['platform'] not in ['windows']:
    Return()

env = env.Clone()

glsl = env.StaticLibrary(
    target = 'glsl',
    source = [
        'sl_pp_process.c',
        'sl_pp_purify.c',
        'sl_pp_token.c',
        'sl_pp_version.c',
    ],
)
Export('glsl')
