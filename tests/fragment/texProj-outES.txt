#extension GL_EXT_shader_texture_lod : enable
#extension GL_EXT_shadow_samplers : enable
varying highp vec4 uv;
uniform sampler2DShadow shadowmap;
uniform sampler2D tex;
void main ()
{
  gl_FragColor = (((((texture2DProj (tex, vec3(uv.xy, uv.w)) + texture2DProj (tex, vec3(uv.xy, uv.z))) + texture2DProjLod (tex, vec3(uv.xy, uv.w), 1.00000)) + texture2DProjLod (tex, vec3(uv.xy, uv.z), 1.00000)) + vec4(shadow2DEXT (shadowmap, vec3(uv.xy, uv.z)))) + vec4(shadow2DProjEXT (shadowmap, vec4(uv.xy, uv.z, uv.w))));
}

