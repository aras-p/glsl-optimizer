uniform sampler2D unity_LightmapInd;
uniform sampler2D unity_Lightmap;
uniform sampler2D _MainTex;
uniform sampler2D _LightBuffer;
void main ()
{
  vec3 tmpvar_1;
  vec3 tmpvar_2;
  tmpvar_1 = gl_TexCoord[1].xyz;
  tmpvar_2 = gl_TexCoord[3].xyz;
  vec4 light_3;
  float x_4;
  x_4 = (fract(((tmpvar_1.y + (tmpvar_1.z * 0.100000)) * 5.00000)) - 0.500000);
  if ((x_4 < 0.00000)) {
    discard;
  };
  light_3 = -(log2(texture2DProj (_LightBuffer, gl_TexCoord[2])));
  light_3.xyz = (light_3.xyz + mix ((2.00000 * texture2D (unity_LightmapInd, tmpvar_2.xy).xyz), (2.00000 * texture2D (unity_Lightmap, tmpvar_2.xy).xyz), vec3(clamp (tmpvar_2.z, 0.00000, 1.00000))));
  vec4 c_5;
  c_5.xyz = (texture2D (_MainTex, gl_TexCoord[0].xy).xyz * light_3.xyz);
  c_5.w = 0.00000;
  gl_FragData[0] = c_5;
}

